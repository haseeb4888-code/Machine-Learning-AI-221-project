# CI dependencies: includes runtime + tests + ML validation (DeepChecks)
-r requirements-docker.txt

pandas==2.0.0

pytest==7.4.3
pytest-cov==4.1.0

deepchecks==0.18.1

# Starlette's TestClient relies on httpx's ASGI transport API that changed in httpx 0.28.
# Pin to a compatible version to keep tests stable.
httpx==0.27.2